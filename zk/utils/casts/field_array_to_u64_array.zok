import "./field_to_u64_array" as field_to_u64_array

def main<N, P>(field[N] input) -> u64[P]:
    assert(P == 4 * N)

    u64[P] res = [0; P]

	for u32 i in 0..N do
		u64[4] v = field_to_u64_array(input[i])
		for u32 j in 0..4 do
			res[i * 4 + j] = v[j]
		endfor
	endfor

	return res